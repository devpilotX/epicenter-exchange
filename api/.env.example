# Epicenter Exchange API — environment variables
# Copy to .env and fill in real values. NEVER commit .env to git.
DB_PATH=/data/epicenter.db

# ============================================================
# SMTP — RECOMMENDED: Resend (3,000 emails/month free, branded
# @epicenterexchange.com, excellent inbox placement)
# Sign up: https://resend.com  -> verify domain -> API Keys -> create
# ============================================================
SMTP_HOST=smtp.resend.com
SMTP_PORT=465
SMTP_USER=resend
SMTP_PASS=re_xxxxxxxxxxxxxxxxxxxxxxxx   # paste the API key from Resend (starts with re_)
FROM_EMAIL=hello@epicenterexchange.com
FROM_NAME=Epicenter Exchange

# ============================================================
# Alternative 1 — Hostinger Business mailbox (only if Hostinger
# allowed you to claim the free email on epicenterexchange.com)
# ============================================================
# SMTP_HOST=smtp.hostinger.com
# SMTP_PORT=465
# SMTP_USER=hello@epicenterexchange.com
# SMTP_PASS=your-mailbox-password
# FROM_EMAIL=hello@epicenterexchange.com
# FROM_NAME=Epicenter Exchange

# ============================================================
# Alternative 2 — use existing paisareality.com mailbox
# (no Hostinger website-slot dance needed; brand still shows
# as "Epicenter Exchange" in recipient inbox)
# ============================================================
# SMTP_HOST=smtp.hostinger.com
# SMTP_PORT=465
# SMTP_USER=hello@paisareality.com
# SMTP_PASS=your-mailbox-password
# FROM_EMAIL=hello@paisareality.com
# FROM_NAME=Epicenter Exchange

# Where admin copies of contact submissions land
NOTIFY_EMAIL=dipanshu@paisareality.com

# Set only for local dev
# ALLOW_LOCALHOST=1
